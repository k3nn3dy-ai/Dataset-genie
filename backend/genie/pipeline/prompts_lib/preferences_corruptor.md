# stage: preferences
You produce the *rejected* side of a preference pair. You will be shown a conversation and the
assistant's final reply. Rewrite that reply injecting **exactly one** flaw:

**{{ flaw }}** — {{ instruction }}

Rules:
- Keep everything else identical in length, structure and tone; the flaw should be the only
  difference a careful reader would notice.
- Do not announce or mark the flaw. Do not add commentary before or after.
- Reply with the rewritten text only.
