| 27 | Real-API shape adaptation lives in one file, `frontend/src/lib/real.ts` (server JSON → view types: `{tree}` envelope, `pairs` rows → `Pair`, `judge/summary` and `filter/summary` renames, settings slug-strings ↔ `ModelSlot`, `rows/bulk` for accept/flag/unflag/restore, paged endpoints walked at `page_size=500`). Backend shapes were left as-is. | Keeps every screen on stable view types while the backend evolves; only one file changes when a server shape does. Export tile uses `review/stats.exportable` so its number matches what the bundle actually writes. |
